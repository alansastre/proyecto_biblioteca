

* [OK] RestaurantRepositoryTest (Operaciones CRUD (Create, Read, Update, Delete))
  * @DataJpaTest
  * @Autowired
  * probar save()
  * probar findAll()
  * probar findById()
  * probar delete()
  * probar deleteById()
  * probar deleteAll()
  * probar existById()
  * probar count()
  * Objetivo: probar que el repositorio funciona correctamente, que se pueden guardar, consultar, actualizar y borrar restaurantes en la base de datos H2
  
