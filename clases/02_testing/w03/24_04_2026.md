# Viernes 24 abril 2026 — Testing Spring

* Repaso de la semana

* Ver commits de grupos y resolver dudas de proyectos
  * Repartir entidades 
  * Implementar entidades
  * Implementar repos

* Consolidar Test de controlador RestaurantControllerTest con MockMvc
  * `restaurantsEmpty` con repositorio vacío
  * `restaurantDetailIsPresentTrue` con id existente
  * `restaurantDetailIsPresentFalse` con id inexistente
  * `restaurantDetailWithDishes` con platos asociados al restaurante
  * @Transactional para cada test de controlador que modifique la base de datos (si no se hace rollback, el orden de ejecución de los tests puede afectar al resultado)


Objetivo: 29/04/26 tener ya las entidades en cada proyecto.

## Requisitos de proyectos

Referencia es el proyecto Biblioteca y Restaurantes.

* 4-5 entidades [SE PUEDE HACER YA]
  * 4-8 atributo por entidad
  * 1-2 asociaciones `@ManyToOne` entre entidades
  
* 4-5 Repositorios [SE PUEDE HACER YA]
  * 1-3 métodos findBy personalizados por repositorio

* Tests de repositorio: [SE PUEDE HACER YA]
  * Clase con `@DataJpaTest` por repositorio
  * Test de métodos `findBy*` personalizados
  * Test de `save()` y `deleteAll()` para cada repositorio

* Controladores: [PARCIALMENTE HECHO]
  * Una clase `@Controller` por entidad
  * con métodos 
    * `@GetMapping("/entidades")`
    * `@GetMapping("/entidades/{id}")`
    * `@GetMapping("/entidades/new")`
    * `@PostMapping("/entidades")`
    * `@GetMapping("/entidades/{id}/edit")`
    * `@GetMapping("/entidades/{id}/delete")`

* Tests de controlador:
  * Clase con `@SpringBootTest` + `@AutoConfigureMockMvc` por controlador
  * testear cada método del controlador con `MockMvc`:

* Capa Service para lógica de negocio y cálculos

* Seguridad con Spring Security (opcional, si da tiempo)

* API REST con `@RestController` y test con `MockMvc` (opcional, si da tiempo)

* Documentación con Swagger (opcional, si da tiempo)









## Eje del día

Cierre de la **semana 3** en el hilo de Testing. Consolidación de `MockMvc` sobre el detalle y preparación del repositorio de equipo para arrancar la semana 4 con tests reales de vuestro dominio.

## Contenido previsto

1. **Completar `restaurantsEmpty`**
   Test ya esqueletizado el martes: `deleteAll()` + `mockMvc.perform(get("/restaurants"))` con `model().attribute("restaurants", hasSize(0))`.

2. **Primer test de detalle**
   * Caso **id válido**: `status().isOk()`, `view().name("restaurant-detail")`, `model().attributeExists("restaurant")`, y ahora también `model().attribute("dishes", ...)`.
   * Caso **id inexistente**: `status().is3xxRedirection()` + `redirectedUrl("/restaurants")`.

3. **`DishRepositoryTest` — repaso**
   Revisar el `@DataJpaTest` existente y los `findBy*` sobre `Dish`, para sostener los tests de controlador que dependen de esos métodos.

4. **Alineación con los equipos**
   * Revisar que cada grupo tiene entidad + repositorio + primer test en su repo.
   * Fijar **convención de PR**: `mvn test` en verde antes de abrir; revisión cruzada.

## Cierre de semana 3 (hilo Testing)

| Día         | Tema                                                                             |
|-------------|-----------------------------------------------------------------------------------|
| Lun 20      | `HelloController`, `RestaurantController`, datos demo, montaje de MockMvc        |
| Mar 21      | `restaurantsFull` con MockMvc; detalle de restaurante con redirect                |
| Mié 22      | Enum en Thymeleaf (`th:switch`) y carta de platos en el detalle                   |
| Vie 24      | `restaurantsEmpty`, test de detalle (OK + redirect) y alineación de equipos      |

## Commit del día (demo o repo de equipo)

```
test: restaurantsEmpty y detalle (200 + 3xx); docs: criterios de PR
```
