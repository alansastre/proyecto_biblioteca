# Lunes 20 abril 2026 — Testing Spring

## Eje del día

Salto de `@DataJpaTest` (capa repositorio, ya cubierto) a **pruebas de controlador** con `MockMvc`. En la misma sesión se construyen los controladores que se van a probar.

## Commits del día en `restaurantes-testing`

* `c26f31d` — `HelloController` con `@GetMapping("/hello")`.
* `addea12` — `GetMapping("/adios")` pasando variables con `Model`.
* `bf55ff6` — `RestaurantController` + `restaurant-list.html`.
* `db774c2` — Datos demo en `main()` (restaurantes de ejemplo para la UI).
* `c9cd37d` — Configuración inicial de `RestaurantControllerTest`.

## Contenido

1. **`HelloController`**
   Primer `@GetMapping`; segunda ruta `/adios` con `model.addAttribute(...)` para fijar cómo se pasan variables a la plantilla.

2. **`RestaurantController` con Lombok**
   Clase anotada `@Controller` y `@AllArgsConstructor` (constructor inyectado por Lombok). Método `@GetMapping("/restaurants")` → `findAll()` → plantilla `restaurant-list`.

3. **Datos demo en `main()`**
   Tras `SpringApplication.run`, se obtiene el repositorio y se guardan restaurantes de ejemplo para que el navegador y los tests tengan datos.

4. **`RestaurantControllerTest` — montaje**
   * `@SpringBootTest` + `@AutoConfigureMockMvc`.
   * `@Autowired MockMvc mockMvc;`
   * `@BeforeEach` con `deleteAll` + `saveAll` de tres restaurantes conocidos para dejar un escenario controlado.

## Pirámide de pruebas (contexto)

* **Repositorio** — ya cubierto con `@DataJpaTest` en semanas previas.
* **Controlador** — desde hoy, **`MockMvc`** (petición HTTP simulada, sin levantar servidor).

## Práctica

* Abrir en el navegador `/hello`, `/adios` y `/restaurants`.
* Revisar el `@BeforeEach` del test y entender por qué se vacía y rellena el repositorio antes de cada prueba.

## Commit del día (demo)

```
feat: HelloController, RestaurantController, datos demo en main; test: montaje MockMvc
```
