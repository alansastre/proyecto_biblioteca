# Miércoles 22 abril 2026 — Testing Spring

## Eje del día

Enriquecer la **vista de detalle** (enum legible + carta de platos) para tener más contenido sobre el que después escribir pruebas de modelo/vista.

## Commits del día en `restaurantes-testing`

* `72d7ad3` — `restaurant-detail.html`: presentación del enum `FoodType` con `th:switch` / `th:case`.
* `3b34580` — Carta de platos asociada al restaurante en el detalle.

## Contenido

1. **Enum en Thymeleaf**
   Presentar `FoodType` (SPANISH, JAPANESE, …) con un bloque `th:switch` → `th:case` que pinta el texto legible en la UI, como alternativa al patrón de `label` del enum.

2. **Carta de platos en el detalle**
   * Inyección de `DishRepository` en `RestaurantController`.
   * `List<Dish> platos = dishRepository.findByRestaurant_Id(id);`
   * `model.addAttribute("dishes", platos);`
   * `th:each` en la plantilla para renderizar la carta.
   * Se dejan TODOs en el controlador para `Employee`, `Review` y `Order` (días siguientes / semana 4).

## Puente con los tests

Con más atributos en el `Model` (`restaurant`, `dishes`) se amplía el catálogo de cosas que se pueden comprobar en `MockMvc` (`model().attribute("dishes", hasSize(n))`, fragmentos de HTML, etc.).

## Organización de los equipos

Sigue en paralelo el trabajo de **repositorio de grupo** (temática + `gx_testing` + issues para el espejo MVC en el proyecto de cada equipo).

## Práctica

* Probar el detalle con un restaurante con y sin platos.
* Añadir una aserción `model().attribute("dishes", hasSize(...))` al test existente.

## Commit del día (demo)

```
feat: enum FoodType con switch en Thymeleaf; carta de platos en restaurant-detail
```

## Montamos grupos 

Hemos creado los grupos en github: 

* g1_testing
* g2_testing
* g3_testing

Hemos creado Issues en GitHub.