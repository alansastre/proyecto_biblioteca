# Martes 21 abril 2026 — Java Spring

## Eje del día

Del listado al **detalle**: URL con `{id}`, `@PathVariable`, navegación con `th:href` y carga de la entidad relacionada (platos del restaurante).

## Commits del día en `restaurantes-java`

* `42fcc32` — Pasos para completar controladores (guion en `ENTIDADES.md` + `CONTROLLERS.png`).
* `52f5f2b` — Imprimir `numRestaurants` en `restaurant-list`.
* `c3874e1` — Enlace `th:href` a `/restaurants/{id}` desde cada fila.
* `513fab3` — `restaurantDetail` en el controlador + `restaurant-detail.html`.
* `f1aa62e` — Cargar platos del restaurante (inyección de `DishRepository`).
* `55c149b` — `th:each` sobre `dishes` en `restaurant-detail.html`.
* `443ce55` — Label en el enum `DishType` para texto legible en la vista.

## Contenido

1. **Guion de controladores**
   Bloque en `ENTIDADES.md` con la lista de métodos típicos de un controller MVC: `findAll`, `findById`, `delete`, `createForm` + `create`, `editForm` + `edit`. Pega `CONTROLLERS.png` como mapa mental.

2. **Listado enriquecido**
   * `model.addAttribute("numRestaurants", ...)` y `model.addAttribute("title", ...)`.
   * Enlace por fila con `th:href="@{/restaurants/{id}(id=${r.id})}"`.

3. **Detalle con `@PathVariable`**
   * `@GetMapping("/restaurants/{id}")`.
   * `Optional<Restaurant> restaurantOptional = restaurantRepository.findById(id);`
   * Si existe → `model.addAttribute("restaurant", ...)` y `return "restaurants/restaurant-detail";`
   * Si no → `return "redirect:/restaurants";` (redirect al controlador, no al HTML).

4. **Platos del restaurante en la ficha**
   * Inyección adicional de `DishRepository` en el `RestaurantController`.
   * Consulta `findByRestaurantIdOrderByPrice(id)` y `model.addAttribute("dishes", platos)`.
   * `th:each` en la plantilla para listar los platos.

5. **Enum con texto legible**
   `DishType` con campo `label` (p. ej. `STARTER("Entrantes")`) para pintar en Thymeleaf un texto de interfaz en vez del nombre crudo del enum.

## Práctica

* Probar `/restaurants/1`, `/restaurants/2` e introducir un `id` inexistente para ver el redirect.
* Comprobar el enlace en el listado y la carta de platos en la ficha.

## Commit del día (demo)

```
feat: detalle de restaurante con platos; th:href, @PathVariable y label de DishType
```
