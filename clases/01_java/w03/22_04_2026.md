# Miércoles 22 abril 2026 — Java Spring

## Eje del día

Segundo controlador MVC siguiendo el mismo patrón que el de restaurantes, aplicado al recurso **plato** (`Dish`).

## Commit del día en `restaurantes-java`

* `f516760` — **`DishController`** con detalle por id + plantilla `dishes/dish-detail.html`.

## Contenido

1. **`DishController`**
   * Clase `@Controller` en el paquete `controller`.
   * Inyección de `DishRepository` por constructor.
   * `@GetMapping("/dishes/{id}")` con `@PathVariable Long id` y `Model`.
   * Mismo patrón de `Optional` + `findById`:
     * presente → `model.addAttribute("dish", dish)` y `return "dishes/dish-detail";`
     * ausente → `return "redirect:/restaurants";`

2. **Plantilla `dishes/dish-detail.html`**
   Bajo `templates/dishes/`, muestra los campos de `Dish` (`name`, `description`, `price`, `type`).

3. **Consolidación del patrón MVC**
   Se deja perfilada la siguiente tarea sobre el detalle de restaurante (pendiente en `ENTIDADES.md`): hacer que cada fila de la carta de platos sea clicable y enlace a `/dishes/{id}`.

## Pistas para el repositorio de equipo

En paralelo al demo se arranca la conversación sobre el **repositorio de cada grupo** y la temática de producto (pendiente de cierre el jueves).

## Práctica

* Añadir en `restaurant-detail.html` el `th:href` sobre el nombre del plato para llegar al nuevo detalle.
* Probar `/dishes/{id}` con un id válido y con uno inexistente (redirect).

## Commit del día (demo)

```
feat: DishController (detalle) y plantilla dishes/dish-detail
```
