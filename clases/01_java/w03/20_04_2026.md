# Lunes 20 abril 2026 — Java Spring

## Eje del día

Arranque del **patrón MVC**: del repositorio en consola al primer listado en el navegador.

## Commits del día en `restaurantes-java`

* `296d8f9` — prueba de `calculateTotalPrice` en `OrderLineRepository` (cierre del hilo de pedidos/líneas de la semana 2).
* `4b3d08c` — `HelloController`, `hola.html`, `adios.html`.
* `3c7886d` — `RestaurantController` y `restaurant-list.html`.

## Contenido

1. **Cierre de `OrderLineRepository`**
   Ejecución desde `main()` del método `calculateTotalPrice` sobre pedidos guardados la semana anterior, para confirmar que la consulta agregada devuelve el importe esperado.

2. **Primer controlador MVC — `HelloController`**
   Clase anotada con `@Controller` y método con `@GetMapping("/hello")` devolviendo un `String` con el nombre de la plantilla (`hola`). Segunda ruta `/adios` para fijar la idea. Se introduce la carpeta `src/main/resources/templates/` y la necesidad de `spring-boot-starter-thymeleaf`.

3. **`RestaurantController` + `restaurant-list.html`**
   * Inyección de `RestaurantRepository` por constructor.
   * `@GetMapping("/restaurants")` con `findAll()` y `model.addAttribute("restaurants", ...)`.
   * Plantilla con `th:each` sobre la lista para pintar la tabla.

4. **Datos demo desde `main()`**
   En el repositorio de referencia los datos de prueba se siguen creando en el `main()` tras `SpringApplication.run` (no hay `CommandLineRunner` ni `DataInitializer`).

## Flujo

```text
Navegador → @GetMapping → Repository.findAll() → Model → Thymeleaf (restaurant-list)
```

## Práctica

* Arrancar la aplicación y visitar `/hello`, `/adios` y `/restaurants`.
* Revisar en `RestaurantesJavaApplication.main` cómo se siembran los datos.

## Commit del día (demo)

```
feat: HelloController, RestaurantController y primer listado Thymeleaf
```
