# Jueves 23 abril 2026 — Java Spring

## Eje del día

Sesión **sin código nuevo** en `restaurantes-java`. Se dedica a organizar los **repositorios de equipo** y el **tablero de issues** para poder arrancar la semana 4 aplicando el patrón MVC al dominio de cada grupo.

## Puntos de la sesión

* **Repositorios por grupo**: clonación, README por temática, convención de ramas y de commits.
* **Diagrama de entidades inicial** de cada proyecto, con asociaciones básicas (`@ManyToOne`).
* **Issues mínimos** para abrir la semana 4:
  1. Entidad principal + `JpaRepository`.
  2. Carga de datos de prueba (`main()` u otro mecanismo acordado).
  3. Primer listado web con `@GetMapping` + Thymeleaf.
  4. Detalle por id con `@PathVariable` + `Optional`.

## Cierre de semana 3 (hilo Java)

| Día         | Tema                                                                                   |
|-------------|-----------------------------------------------------------------------------------------|
| Lun 20      | `HelloController`, `RestaurantController`, primer listado con Thymeleaf                |
| Mar 21      | Detalle de restaurante, `@PathVariable`, enlaces y carta de platos en la ficha         |
| Mié 22      | `DishController` (detalle) y plantilla `dishes/dish-detail`                            |
| Jue 23      | Repositorios de equipo, issues y diagrama de dominio (sin commits en el demo)          |

## Deberes hacia la semana 4

* Tener **clonado** el repo del grupo y leído el README de la temática.
* Al menos una **entidad + repositorio** acordados y un *issue* abierto para el primer `GetMapping` de listado.

## Commit del día (repo de equipo)

```
docs: diagrama de entidades inicial; chore: tablero de issues para la semana 4
```
